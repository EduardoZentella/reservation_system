[project]
name = "reservation-system"
version = "0.0.2"
description = "Personal reservation system implemented with red-black tree"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.116.1",
]
